// Visual spindle speed for the sim tool indicator in both the 3D view and the
// 2D canvas (rev/s). Slow enough to read the flutes; real RPM would be a blur.
// Standard M3 rotation is clockwise viewed from above the workpiece.
export const SPINDLE_VIS_RPS = 2.0
